<table class="promotion_code">
  <tr>
    <th>Code</th>
    <th>Discount Amount</th>
  </tr>
  <% @promotion_codes.each do |promotion_code| %>
    <% if !promotion_code.used %>
      <tr>
        <td><%= promotion_code.code %></td>
        <td>¥<%= promotion_code.discount_amount %></td>
      </tr>
    <% end %>
  <% end %>
</table>
