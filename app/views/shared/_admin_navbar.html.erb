<div class="navigation">
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <%= link_to "Admin Page", admins_path, class:"navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "ホーム", admins_path, class: "nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "新規作成", new_admin_path, class: "nav-link active" %>
          </li>
        </ul>
        <% if @current_user %>
          <%= form_with url: logout_path, method: :delete, class: "d-flex" do |f| %>
            <%= f.submit "ログアウト", class: "btn btn-outline-success" %>
          <% end %>
        <% end %>
      </div>
    </div>
  </nav>
</div>
